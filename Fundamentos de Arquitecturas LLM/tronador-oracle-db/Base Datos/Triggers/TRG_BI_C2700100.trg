CREATE OR REPLACE TRIGGER TRG_BI_C2700100
BEFORE INSERT
ON C2700100
REFERENCING NEW AS NEW OLD AS OLD
FOR EACH ROW
DECLARE
  merror     VARCHAR2( 500 ) := NULL;
  vmsgerror  VARCHAR2( 2000 );


BEGIN
  :NEW.NUM_POL_CLI :=SUBSTR(:NEW.NUM_POL1,5,7);
END;
/
