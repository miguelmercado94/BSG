# Producción: importa QA; sin SQL init en arranque; Redis obligatorio (revocación en caché).
# Activación: --spring.profiles.active=pdn

spring:
  sql:
    init:
      mode: never

bsg:
  security:
    deployment: pdn
    redis:
      enabled: true
    auth:
      reset-password-base-url: ${BSG_SECURITY_AUTH_RESET_PASSWORD_BASE_URL:https://app.example.invalid/reset-password}
    aws:
      dynamodb:
        endpoint: "" # Forzamos a que use el endpoint real de AWS, no localstack

cloud:
  aws:
    region:
      static: us-east-1  # Es mejor usar .region.static

management:
  endpoint:
    health:
      show-details: never
  health:
    redis:
      enabled: true

logging:
  level:
    root: WARN
    com.bsg.security: INFO
    org.springframework.r2dbc.core: WARN
    org.springframework.data.r2dbc: WARN
    io.r2dbc: WARN
