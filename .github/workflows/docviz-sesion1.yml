name: DocViz Sesión 1 - Docker Hub Versionado

on:
  push:
    branches: 
      - BSG-DIE-SESION1
      - main
  pull_request:
    branches: 
      - main

concurrency:
  group: docviz-sesion1-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # Ruta base (Asegúrate de que 'diseño-infraestructura-escalable' sea el nombre real en GitHub)
  SESION_ROOT: "diseño-infraestructura-escalable/sesion1"

jobs:
  changes:
    name: Detectar cambios
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      frontend: ${{ steps.filter.outputs.frontend }}
      security: ${{ steps.filter.outputs.security }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - '**/backend-sesion1/**'
            frontend:
              - '**/frontend-sesion1/**'
            security:
              - '**/back-security-sesion1/**'

  test-java:
    needs: changes
    if: needs.changes.outputs.backend == 'true' || needs.changes.outputs.security == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Run Maven Build
        run: |
          # Buscamos el POM dinámicamente para evitar errores de ruta estática
          if [ "${{ needs.changes.outputs.backend }}" == "true" ]; then
            BACKEND_POM=$(find . -name "pom.xml" | grep "backend-sesion1" | head -n 1)
            echo "Construyendo backend en: $BACKEND_POM"
            mvn -f "$BACKEND_POM" clean compile -DskipTests
          fi
          
          if [ "${{ needs.changes.outputs.security }}" == "true" ]; then
            SECURITY_POM=$(find . -name "pom.xml" | grep "back-security-sesion1" | head -n 1)
            echo "Construyendo seguridad en: $SECURITY_POM"
            mvn -f "$SECURITY_POM" clean compile -DskipTests
          fi

  build-and-push:
    needs: [changes, test-java]
    if: |
      always() && 
      github.event_name == 'push' && 
      (needs.test-java.result == 'success' || needs.test-java.result == 'skipped')
    runs-on: ubuntu-latest
    strategy:
      matrix:
        project: [back-security-sesion1, backend-sesion1, frontend-sesion1]
    
    steps:
      - uses: actions/checkout@v4

      - name: Check Project Change
        id: check
        run: |
          SHOULD_RUN="false"
          # Usamos comodines para ser flexibles con la ubicación de las carpetas
          if [[ "${{ matrix.project }}" == "back-security-sesion1" && "${{ needs.changes.outputs.security }}" == "true" ]]; then SHOULD_RUN="true"; fi
          if [[ "${{ matrix.project }}" == "backend-sesion1" && "${{ needs.changes.outputs.backend }}" == "true" ]]; then SHOULD_RUN="true"; fi
          if [[ "${{ matrix.project }}" == "frontend-sesion1" && "${{ needs.changes.outputs.frontend }}" == "true" ]]; then SHOULD_RUN="true"; fi
          echo "run=$SHOULD_RUN" >> $GITHUB_OUTPUT

      - name: Login to Docker Hub
        if: steps.check.outputs.run == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker Build and Push
        if: steps.check.outputs.run == 'true'
        run: |
          # Buscar la ruta real de la carpeta del proyecto
          REAL_PATH=$(find . -type d -name "${{ matrix.project }}" | head -n 1)
          VERSION=$(cat "$REAL_PATH/version.txt" | xargs)
          REPO_NAME="${{ secrets.DOCKERHUB_USERNAME }}/${{ matrix.project }}"
          
          echo "Construyendo imagen desde: $REAL_PATH"
          
          docker build -t "$REPO_NAME:$VERSION" -t "$REPO_NAME:latest" -f "$REAL_PATH/Dockerfile" "$REAL_PATH"
          docker push "$REPO_NAME:$VERSION"
          docker push "$REPO_NAME:latest"